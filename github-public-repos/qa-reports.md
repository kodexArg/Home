---
id: "qa-reports"
title: "qa-reports — Public HTML archive for multi-agent QA audit deliverables"
visibility: public
importance: normal
source_repo: "qa-reports"
org: "kodexArg"
default_branch: "main"
primary_language: "HTML"
repo_kind: "documentation"
status: "active"
related: []
tags: ["qa", "audit", "html", "reports", "security", "accessibility", "performance", "agents", "static-site", "kodexarg"]
problems_solved:
  - "QA audit findings produced by the kodexArg QA workspace had no durable, shareable public home—stakeholders could not review launch-readiness verdicts without access to the private agent sandbox."
  - "Multi-dimensional audit output (ten parallel agent domains, severity scoring, remediation tiers) lacked a single consistent visual contract, making comparisons across projects and audit runs unreliable."
  - "Automated agent report generation needed a git-backed publish target that tracks only finished HTML artifacts and excludes workspace clutter, clones, and transient credentials."
technologies:
  - "Static HTML5 + embedded CSS"
  - "Google Fonts (Space Grotesk, DM Mono)"
  - "Mermaid 11 (workflow documentation page only)"
  - "Git-tracked HTML-only publishing (.gitignore whitelist)"
  - "PostToolUse auto-commit hook (upstream QA workspace)"
generated_by: "github-repo-swarm"
generated_note: "Single verbose summary markdown; not a dump of every source file."
---

# qa-reports

> **Problem thesis (required):** The kodexArg QA workspace runs ten parallel audit agents against cloned repositories and live deployments, producing dense findings across security, accessibility, performance, SEO, code quality, functionality, dependencies, compliance, operations, and framework best practices. Those deliverables must survive outside the private sandbox: clients, operators, and future agents need a stable, browsable archive of finished audit reports with uniform scoring, severity taxonomy, and launch verdicts. This repository is that public sink—HTML-only, auto-published from the QA workspace, with a canonical template and an internal workflow reference page—so audit knowledge is portable without exposing the testing sandbox or cloned target repos.

## 1. Identity

| Field | Value |
|-------|-------|
| Org / repo | `kodexArg/qa-reports` |
| Visibility | `public` |
| Default branch | `main` |
| One-line pitch | Public HTML-only archive of QA audit reports generated by the kodexArg QA agent workspace, with a shared visual template and auto-publish pipeline. |
| Audience | Project owners and operators reviewing launch-readiness; kodexArg internal QA agents publishing finished audits; stakeholders who need shareable audit artifacts without cloning the QA workspace; RAG/indexing systems cataloging audit history across audited projects. |

## 2. Problems it solves

### P1 — No durable public home for QA deliverables

- **Who hurts:** Operators running full or partial audits in the private `kodexArg/QA` workspace; project owners who need to circulate findings; anyone blocked from the gitignored `github-repositories/` clone sandbox.
- **Pain today:** Audit output lived only on the operator machine inside a nested `reports/` directory. Sharing meant ad-hoc file transfer or granting workspace access. Historical audits were hard to discover across machines and sessions.
- **How this repo answers:** Finished reports are written as self-contained HTML files and auto-committed here via a PostToolUse hook in the upstream QA workspace. The repo tracks **only** `.html` files (plus `.gitignore`), so every push is a clean artifact drop. Filename convention `YYYY-MM-DD_owner-repo_audit-type.html` makes chronology and target project obvious at a glance.
- **Out of scope:** Storing cloned target repositories, Python automation scripts, agent skills, or markdown companions (`.md` reports are generated locally but explicitly excluded by `.gitignore` here).

### P2 — Inconsistent audit presentation across runs and projects

- **Who hurts:** Readers comparing audits across `luinloder-RRHH`, `luinloder-SDGD`, `kdx-ng-template`, and future targets; agents compiling the next report; operators explaining severity and verdict logic to non-technical stakeholders.
- **Pain today:** Without a enforced layout, each audit could drift in structure, scoring display, and remediation grouping—undermining trust in cross-project comparisons and making automated ingestion harder.
- **How this repo answers:** `template.html` defines the canonical report skeleton: header metadata, ten category score cells with progress bars, severity strip and legend, ten audit sections (Security through Framework), per-finding blocks with ID prefixes and severity badges, strengths grids, pages-tested and summary tables, weighted launch verdict, and tiered action plans (immediate / short / medium / backlog). Generated reports inline the compiled CSS so each file opens standalone in any browser. `qa-help-and-workflow.html` documents the full pipeline, agent roster, scoring formula, and naming rules in the same design system.
- **Out of scope:** Interactive dashboards, search indexes, or API access to findings—the surface is static HTML only.

### P3 — Separating audit artifacts from the testing sandbox

- **Who hurts:** QA operators who must never accidentally commit cloned client repos; security reviewers worried about sandbox leakage; CI systems that should only see publishable output.
- **Pain today:** A monorepo mixing clones, scripts, context PDFs, and reports risks pushing sensitive or bloated trees. Per-repo gitignore entries are error-prone when many targets are cloned.
- **How this repo answers:** The QA workspace keeps clones under gitignored `github-repositories/` and publishes **only** HTML to this separate public repo. The `.gitignore` here uses an aggressive whitelist (`*` then `!*.html` and `!.gitignore`), guaranteeing no stray workspace files enter the public archive. The workflow doc (`qa-help-and-workflow.html`) states the critical rule explicitly: cloned repos are never committed from the QA workspace.
- **Out of scope:** Private or redacted report variants; per-client access control (everything published here is public).

## 3. Product / idea

This repository is a **read-only publication layer**, not an application or audit runner. The mental model is a funnel:

```
kodexArg/QA workspace (private)
  → 10 parallel audit agents + scoring
  → reports/YYYY-MM-DD_owner-repo_type.html (local)
  → PostToolUse hook: git add, commit, push
kodexArg/qa-reports (public, this repo)
  → browsable HTML archive
```

Each published file is a **self-contained audit dossier**. A typical full audit opens with project identity (name, subtitle, date, target deployment, source repository reference, auditor label, stack summary, scope list, auth notes when applicable), then renders category scores on a 0–10 scale with color-coded bars. Findings appear in domain sections ordered by severity, each tagged with a stable ID prefix (`S-xx` Security, `A-xx` Accessibility, `P-xx` Performance, `E-xx` SEO, `C-xx` Code, `FN-xx` Functional, `D-xx` Dependencies, `R-xx` Compliance, `O-xx` Operations, `FW-xx` Framework). The report closes with a launch verdict (`READY`, `READY WITH RESERVES`, or `NOT READY`), narrative verdict note, and prioritized remediation lists.

`template.html` retains `{{PLACEHOLDER}}` tokens for agent-driven compilation—project name, dates, scores, finding blocks, verdict class, and action tiers—serving as the SSOT layout spec. `qa-help-and-workflow.html` is the operator-facing companion: Mermaid diagrams for the seven-phase workflow, the ten-agent parallel fan-out, authentication tiers, scoring formulas, folder structure of the upstream QA workspace, and browser-testing conventions (Brave CDP + `agent-browser`).

Current archive contents (as of clone) span early 2026 audits for HR and SDGD projects under the `luinloder` owner prefix and a March 2026 full audit for `kdx-ng-template`, plus iterative re-runs on the same target where remediation was re-tested.

### 3.1 North-star use cases

1. **Publish after full audit:** QA agent completes Phase 7 in the upstream workspace, writes `reports/2026-03-13_kodexarg-kdx-ng-template_full-audit.html`, hook auto-pushes; stakeholders open the file on the public repo tree or raw view.
2. **Onboard a new operator:** Human or agent reads `qa-help-and-workflow.html` to learn required inputs (GitHub repo URL + live site URL), parallel agent roles, scoring weights, verdict thresholds, and report naming before starting an audit in `kodexArg/QA`.
3. **Compare audit history:** Multiple `luinloder-RRHH` files (`2026-02-27` through `2026-03-01`, including `full-audit-v4` and a `performance`-only run) document iteration on the same target across dates and audit types.
4. **Extend the template:** Agent copies `template.html`, fills placeholders, inlines findings HTML fragments, and produces a new standalone report matching the visual contract.

### 3.2 Non-goals

- Not the QA execution environment—no agents, scripts, skills, or cloned repos live here (those belong in `kodexArg/QA`).
- Not a build pipeline—no bundler, package manager, or CI config in-tree; reports are hand-or-agent-compiled static HTML.
- Not a markdown or JSON findings API—companion `.md` files may be written upstream but are deliberately untracked here.
- Not access-controlled or redacted publishing—visibility is public for all committed HTML.
- Not a live monitoring or regression system—each file is a point-in-time audit snapshot.

## 4. Technology stack

The repository is intentionally minimal: no `package.json`, no Python manifest, no Wrangler or Docker files. Everything is static HTML with embedded CSS (and one page loading Mermaid from a CDN).

| Layer | Choices | Evidence (path, not URL) |
|-------|---------|--------------------------|
| Runtime / language | HTML5, CSS3 (custom properties, grid, flex) | `template.html`, `qa-help-and-workflow.html`, audit `*.html` files |
| Typography | Space Grotesk (UI), DM Mono (labels, code) via Google Fonts `@import` | `template.html` head styles |
| Diagrams | Mermaid 11 (workflow doc only) | `qa-help-and-workflow.html` script tag |
| Frontend framework | None — zero JavaScript in audit reports; Mermaid init only on help page | `template.html` (CSS-only body), `qa-help-and-workflow.html` |
| Backend / API | None | — |
| Data | None — no database, KV, or state | — |
| Infra / deploy | GitHub static file hosting (public repo); auto-push from upstream QA hook | `.gitignore`, `qa-help-and-workflow.html` Section 07 |
| AI / agents | Upstream `kodexArg/QA` workspace (`.claude/`, 11 QA skills, PostToolUse hook) — **not present in this repo** | `qa-help-and-workflow.html` Sections 02–07 (describes upstream) |
| Tests | None in this repo | — |

### 4.1 Notable dependencies (curated)

- **Embedded CSS design tokens** — `:root` severity colors (`--critical`, `--high`, `--medium`, `--low`, `--pass`), verdict colors, and layout measure (`--measure: 72rem`) unify all reports without a shared stylesheet file.
- **Mermaid 11** — renders workflow, auth-tier, browser-setup, and publish sequence diagrams only in `qa-help-and-workflow.html`.
- **Google Fonts** — `DM Mono` and `Space Grotesk` loaded via `@import` in report `<style>` blocks (audit reports remain offline-friendly except font fetch).
- **Upstream agent toolchain (external)** — Python + httpx, BeautifulSoup4, Grep/Read, `agent-browser` + Brave CDP, `uv run` scripts — referenced in workflow doc, not vendored here.

## 5. Repository map (abstraction)

The tree is flat at the repository root—no `src/`, `docs/`, or package directories.

- **Template / SSOT layout:** `template.html` — placeholder-driven master layout for all ten audit sections, score grid, verdict block, and action tiers (~1,060 lines including embedded CSS and HTML comments documenting finding block shape).
- **Operator documentation:** `qa-help-and-workflow.html` — nine sections covering inputs, workflow, agents, auth, scoring, upstream folder structure, auto-publish pipeline, browser testing, and quick reference (~780 lines).
- **Published audit artifacts:** dated `*.html` files at repo root:
  - `2026-02-27_luinloder-RRHH_full-audit.html`
  - `2026-02-27_luinloder-RRHH_full-audit-v4.html`
  - `2026-02-27_luinloder-RRHH_performance.html` (performance-only scope)
  - `2026-02-28_luinloder-RRHH_full-audit.html`
  - `2026-03-01_luinloder-RRHH_full-audit.html`
  - `2026-03-01_luinloder-SDGD_full-audit.html`
  - `2026-03-13_kodexarg-kdx-ng-template_full-audit.html`
- **Publishing contract:** `.gitignore` — tracks only `*.html` and itself; all other paths ignored by default.
- **Agent scaffolding:** `.claude/`, `.docs/`, `docs/`, `README*` — **not present in this repo**; upstream `kodexArg/QA` holds `AGENTS.md`, `.claude/CLAUDE.md`, skills, and hooks per `qa-help-and-workflow.html` Section 06.
- **Generated / vendor:** none committed; external font and Mermaid CDN loads are runtime fetches in browser only.

## 6. Configuration & contracts (no secrets)

### Git tracking whitelist

`.gitignore` contents:

```
# Only track HTML files
*
!*.html
!.gitignore
```

This means markdown companions, env files, credentials, and workspace debris cannot be committed accidentally.

### Report naming convention

`YYYY-MM-DD_github-owner-repo_audit-type.html`

Examples from the tree: `2026-03-01_luinloder-RRHH_full-audit.html`, `2026-02-27_luinloder-RRHH_performance.html`, `2026-03-13_kodexarg-kdx-ng-template_full-audit.html`.

Audit types observed: `full-audit`, `full-audit-v4` (versioned re-run), `performance` (scoped performance-only report).

### Template placeholder contract (`template.html`)

Key tokens agents replace when compiling a report:

| Token group | Examples | Purpose |
|-------------|----------|---------|
| Header | `{{PROJECT_NAME}}`, `{{DATE}}`, `{{PROJECT_DESCRIPTION}}`, `{{TARGET_URL}}`, `{{REPO_URL}}`, `{{AUDITOR}}`, `{{TECH_STACK}}` | Report identity block |
| Scores | `{{SCORE_SECURITY}}`, `{{SCORE_*_PCT}}` for all ten categories | Numeric score + bar width |
| Counts | `{{COUNT_CRITICAL}}`, `{{COUNT_HIGH}}`, `{{COUNT_MEDIUM}}`, `{{COUNT_LOW}}`, `{{COUNT_TOTAL}}` | Severity strip |
| Sections | `{{FINDINGS_SECURITY}}`, `{{STRENGTHS_*}}`, `{{METRICS_PERFORMANCE}}` | Per-domain HTML fragments |
| Summary | `{{PAGES_TABLE}}`, `{{FINDINGS_SUMMARY_TABLE}}` | Coverage tables |
| Verdict | `{{VERDICT}}`, `{{VERDICT_CLASS}}`, `{{VERDICT_NOTE}}` | Launch readiness |
| Actions | `{{ACTIONS_IMMEDIATE}}`, `{{ACTIONS_SHORT}}`, `{{ACTIONS_MEDIUM}}`, `{{ACTIONS_BACKLOG}}` | Remediation tiers |
| Framework | `{{FRAMEWORK_HEADING}}` | Stack-specific section title |

### Scoring contract (from workflow doc)

- **Category score:** `10 − (Critical×2.0 + High×1.0 + Medium×0.4 + Low×0.1)`, clamped to [0, 10].
- **Overall weighted score:** Security 25%, Accessibility 20%, Performance 15%, Code 15%, Functional 15%, SEO 10% (adjustable by risk profile upstream).
- **Verdict A — READY:** overall ≥ 8.0, zero critical, zero unresolved high.
- **Verdict B — READY WITH RESERVES:** overall 6.0–7.9, zero critical, highs have remediation timeline.
- **Verdict C — NOT READY:** overall < 6.0 or any unresolved critical.

### 6.1 HTTP / API endpoints (when applicable)

This repository exposes **no HTTP API** and contains no server, worker, or routing code. Artifacts are static HTML files consumed via Git hosting raw/tree views or local `file://` open.

N/A for method/path tables. The audited *targets* in published reports reference localhost or deployment URLs inside report bodies (upstream agent output); this archive repo itself does not serve them.

### 6.2 Other interfaces

- **Browser viewing:** open any `*.html` directly; print stylesheet included (`@media print` in template).
- **Upstream CLI / agent interface:** full audits start in `kodexArg/QA` with "run full audit" + GitHub repo URL + live site URL; partial audits target single domains ("run security scan on X"). Documented in `qa-help-and-workflow.html` Section 09.
- **Auto-publish hook:** PostToolUse in upstream `.claude/settings.json` triggers `git add *.html`, commit, push to this repo on writes inside `reports/` — described in workflow Section 07, not implemented in this tree.
- **Browser automation contract (upstream):** `agent-browser --cdp 9222` against Brave with `--remote-debugging-port=9222`; functional agent uses this path exclusively per workflow Section 08.

## 7. Data & persistence

- **Stores used:** none. No SQL, KV, object storage, or vector index in this repository.
- **Entities:** each HTML file is an immutable audit snapshot; filename encodes date, owner-repo slug, and audit type. No database schema or migrations.
- **Topology:** single-region static git content. Upstream QA workspace is local/operator machine; this repo is the cloud-visible mirror. Cloned target repos never enter this repo—only derived report HTML.

## 8. Docs & agent memory (required scan)

Scans performed on shallow clone of `kodexArg/qa-reports` (branch `main`):

| Source | Present? | Summary |
|--------|----------|---------|
| Root `README*` | No | No README in repo; operator docs are HTML-based. |
| `docs/**` | No | — |
| `.docs/**` | No | — |
| `.claude/**` | No | Agent instructions live in upstream `kodexArg/QA` per `qa-help-and-workflow.html` tree diagram. |
| `qa-help-and-workflow.html` | Yes | Primary in-repo documentation: inputs, 7-phase workflow, 10 agents, auth tiers, scoring, folder structure, publish pipeline, browser setup, quick reference. |
| `template.html` | Yes | Layout SSOT with placeholder contract and commented finding-block examples. |
| Published audits (`*.html`) | Yes | Evidence of real audits: RRHH (multiple iterations), SDGD, kdx-ng-template; verdicts observed include `NOT READY` on recent full audits. |

**Evidence bullets (paths only):**

- `qa-help-and-workflow.html` — Sections 01–09 define the full QA operating model and this repo's role as `reports/` mirror.
- `template.html` — ten-section audit layout, severity badges, verdict and action-tier structure.
- `2026-03-13_kodexarg-kdx-ng-template_full-audit.html` — exemplar full audit: Angular 21 + Django 5.2 stack, ten category sections, `NOT READY` verdict with blocker narrative.
- `2026-02-27_luinloder-RRHH_performance.html` — exemplar scoped audit (performance-only sections, no full launch verdict block).
- `.gitignore` — HTML-only tracking policy.

## 9. Security & privacy notes (summary-time)

- **Visibility:** public repository. All committed HTML is world-readable. Operators must not publish reports containing secrets, live credentials, or client-private data without redaction upstream.
- **Auth model:** none for consuming this repo. Upstream audits may record tested auth methods inside report bodies (e.g., test email/password labels in metadata)—those are artifacts of the audit process, not access controls here.
- **Secret hygiene:** `.env`, PEM keys, and credential files are excluded by the whitelist gitignore pattern if they ever appeared locally, but operators should never attempt to commit them. Workflow doc states tokens stay in `/tmp/` only during audits.
- **This summary:** contains no secrets, private keys, connection strings, or scraped env contents.

## 10. Operational picture

- **Local use:** clone or open any `*.html` in a browser; no install step, no `bun install`, no build command.
- **Publishing:** automated from `kodexArg/QA` workspace—agent writes to local `reports/`, PostToolUse hook commits and pushes HTML to this repo. Manual edits to this repo are possible but atypical; template changes propagate when operators copy updated `template.html` from QA into both places.
- **Upstream dev commands (external, not in this repo):** `uv run` for Python scripts in QA `scripts/`; `git pull` inside `github-repositories/<target>/` before re-audit; Brave launched with CDP port 9222 for functional tests.
- **Hardware constraints:** none for viewing reports; upstream browser audits assume a machine with Brave and network access to target deployments.

## 11. Open questions / unknowns

- Whether `main` or `master` is the hook push branch upstream—the workflow diagram mentions `master` while this repo's default branch is `main` (clone used `main` successfully).
- No in-repo CI/CD workflow file—publish reliability depends entirely on the upstream PostToolUse hook configuration in `kodexArg/QA`.
- Markdown companion reports (`.md` with same basename as HTML) are generated upstream per workflow Section 07 but intentionally absent here; unknown if a separate archive exists.
- Future report volume and retention policy not documented in-tree.
- Relationship to other kodexArg repos (`kdx-ng-template`, client repos under `luinloder/*`) is implied by filenames only—no manifest indexes audited projects.
