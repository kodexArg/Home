---
import Aurora from '../components/Aurora.svelte';
import { DEFAULT_LANGUAGE } from '../lib/ui/language';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { title = 'kodexArg — LLM Router', description = 'kodexArg LLM Router Interface' } = Astro.props;
---

<!DOCTYPE html>
<!-- First paint uses the app default; the language store re-stamps this on hydration. -->
<html lang={DEFAULT_LANGUAGE}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#141210" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <Aurora client:only="svelte" />
    <slot />
  </body>
</html>
