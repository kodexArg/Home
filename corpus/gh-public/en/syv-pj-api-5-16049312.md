---
id: syv-pj-api-5-16049312
title: "SyV Personajes API — procedural character engine (FastAPI + Cloudflare Worker) — 3. Product / idea"
visibility: public
importance: high
source_repo: "syv-pj-api"
related: ["gh-syv-pj-api"]
tags: ["syv-pj-api", "github", "public", "high", "summary"]
---
## 3. Product / idea

The mental model is **hexagonal architecture around a generation pipeline**: 1. **Domain** ( ) — , , (cuerpo/mente/alma), , ; derived calculators ( , ). 2. **Generation** ( ) — owns phase order; each phase module registers resolvers that read catalog weights and rules from data, not code. makes draws reproducible from a seed. Only the prose phase awaits an async port (LLM boundary). 3. **Ports** ( ) — , , , (wired but lightly used). 4. **Adapters** ( ) — reads submodule resources; ; ; . 5. **API edge** ( ) — FastAPI routers mirror Spanish routes; Pydantic contracts use Spanish field aliases; orchestrates generation, canonization, and events. **Efímero flow:** client calls (or ) with optional , , , repeated filters, and optional pruning → receives a full sheet with → optionally with that payload. **Canonizado flow:** persisted sheet keyed by slug → for current state → for milestones only → for campaign updates. **Meta flow:** clients discover factions, rank attribute tables, tag catalogs ( and subcategory routes like ), milestone type hints, and fresh encounter plates. Production runs the same app module as local dev; the Worker entrypoint ( ) sets and to bundled resources before importing .
